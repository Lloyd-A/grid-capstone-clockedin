--Select Query for a location
SELECT * FROM locations;


--Select Query for a shift
SELECT * FROM shifts WHERE course_enum = 'COMP1161';


--Select Query for users

SELECT * FROM users;

--Select Query for labtech

SELECT * FROM labtechs;

-- Select query for abstract_requests

SELECT * FROM abstract_requests;
